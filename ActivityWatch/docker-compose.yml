version: '3'
services:
  activitywatch:
    build: .
    container_name: activitywatch
    ports:
      - "5600:5600"
    volumes:
      - ./data:/root/.local/share/activitywatch
    restart: unless-stopped